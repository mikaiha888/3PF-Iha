import { AdminsFormValidationsPipe } from './admins-form-validations.pipe';

describe('AdminsFormValidationsPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminsFormValidationsPipe();
    expect(pipe).toBeTruthy();
  });
});
